import { DataTestIdDirective } from './data-test-id.directive';

describe('DataTestIdDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTestIdDirective();
    expect(directive).toBeTruthy();
  });
});
